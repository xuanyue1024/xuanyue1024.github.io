style:
  prefers_theme: auto
  font-family:
        logo: '"LXGW WenKai Screen", system-ui, "Microsoft Yahei", "Segoe UI", Arial, sans-serif'
        body: '"LXGW WenKai Screen", system-ui, "Microsoft Yahei", "Segoe UI", Arial, sans-serif'
        code: 'JetBrains Mono NL, Menlo, Monaco, Consolas, system-ui, monospace, sans-serif'
comments:
  service: giscus
  # giscus
  # https://giscus.app/zh-CN
  giscus:
    src: https://giscus.app/client.js
    data-repo: xuanyue1024/xuanyue1024.github.io # [在此输入仓库]
    data-repo-id: R_kgDONx7jdQ # [在此输入仓库 ID]
    data-category: Announcements # [在此输入分类名]
    data-category-id: DIC_kwDONx7jdc4CrpVg
    data-mapping: pathname
    data-strict: 0
    data-reactions-enabled: 1
    data-emit-metadata: 0
    data-input-position: top # top, bottom
    data-theme: preferred_color_scheme
    data-lang: zh-CN
    data-loading: lazy
    crossorigin: anonymous
inject:
  head:
    - <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    - <link rel="stylesheet" href="https://cdn.staticfile.org/lxgw-wenkai-screen-webfont/1.6.0/lxgwwenkaiscreen.css">

article:
  # 文章推荐
  # npm i hexo-related-popular-posts
  # related_posts:
  #   enable: true
  #   title: 您可能感兴趣的文章
  #分享链接
  share: [wechat, weibo, email, link]
# 备用站点
# 一旦设置源站地址，非源站地址将不会被SEO收录，并且访问时弹出提示
# 如果访问地址不在备用站主机列表，则警告信息为非法克隆
canonical:
  originalHost: yanghc.dev
  officialHosts: # 官方主机列表，每行一个，例如 xaoxuu.vercel.app
    - yanghc.codes
    - localhost
    - yanghc.cn
# 站点主结构树
site_tree:
  # -- 列表类页面 -- #
  # 主页配置
  home:
    leftbar: [recent, ghuser]
    rightbar: welcome

footer:
  social:
    github:
      title: Github
      icon: '<i class="fa-solid fa-github fa-brands fa-bounce"></i>'
      url: https://github.com/xuanyue1024
    music:
      title: Gitee
      icon: '<i class="fa-solid fa-git-alt fa-brands fa-bounce"></i>'
      url: https://gitee.com/xuanyue03
    unsplash:
      title: CSDN
      icon: '<i class="fa-solid fa-ghost fa-brands fa-bounce"></i>'
      url: https://blog.csdn.net/weixin_45564332
    comments:
      title: 哔哩哔哩
      icon: '<i class="fa-solid fa-bilibili fa-brands fa-bounce"></i>'
      url: https://space.bilibili.com/477155658

  content: | # 支持 Markdown 格式
    <center>
    <span>
    © 2024
    <a style="font-weight: bold;  auto;" target="_blank" rel="noopener" href="https://github.com/xuanyue1024">竹林听雨</a>
    使用 <a style="font-weight: bold;  auto;" target="_blank" rel="noopener" href="https://github.com/FelicxFoster/hexo-theme-stellar">Stellar</a>
    创建
    </span>
    <br>
    <!--不蒜子计数器-->
    <script async="" src="//busuanzi.ibruce.info/busuanzi/2.3/busuanzi.pure.mini.js"></script>
    <!--添加一个访问量-->
    <span>总访问 <span id="busuanzi_value_site_pv" style="font-weight: bold;">113</span> 次 | 本页访问 <span id="busuanzi_value_page_pv" style="font-weight: bold;">26</span> 次</span>
    </br>
    <span id="runtime_span"></span>

    <script type="text/javascript">
      document.addEventListener('DOMContentLoaded', function() {
        var runtime_span = document.getElementById('runtime_span');

        function show_runtime() {
          // 调用定时器
          setTimeout(show_runtime, 1000);

          var seconds = 1000;
          var minutes = seconds * 60;
          var hours = minutes * 60;
          var days = hours * 24;
          var years = days * 365;

          var today = new Date();
          var todayYear = today.getFullYear();
          var todayMonth = today.getMonth();
          var todayDate = today.getDate();
          var todayHour = today.getHours();
          var todayMinute = today.getMinutes();
          var todaySecond = today.getSeconds();

          var t1 = Date.UTC(2025, 0, 30, 16, 16, 34); // UTC时间，月份从0开始
          var t2 = Date.UTC(todayYear, todayMonth, todayDate, todayHour, todayMinute, todaySecond);
          var diff = t2 - t1;

          var diffYears = Math.floor(diff / years);
          var diffDays = Math.floor((diff / days) - diffYears * 365);
          var diffHours = Math.floor((diff - (diffYears * 365 + diffDays) * days) / hours);
          var diffMinutes = Math.floor((diff - (diffYears * 365 + diffDays) * days - diffHours * hours) / minutes);
          var diffSeconds = Math.floor((diff - (diffYears * 365 + diffDays) * days - diffHours * hours - diffMinutes * minutes) / seconds);

          // 更新显示时间的span元素内容
          runtime_span.innerHTML = "👋营业：" + diffYears + " 年 " + diffDays + " 天 " + diffHours + " 小时 " + diffMinutes + " 分钟 " + diffSeconds + " 秒🎉";
        }

        show_runtime(); // 初次调用函数以启动定时器
      });
    </script>
    </center>
